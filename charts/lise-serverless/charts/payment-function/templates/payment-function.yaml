apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: payments
spec:
  template:
    spec:
      containers:
        - image: nomost80/payment-function:0.1.0
          env:
            - name: KAFKA_HOST
              value: {{ include "kafka.endpoint" . }}