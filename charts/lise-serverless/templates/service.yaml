apiVersion: v1
kind: Service
metadata:
  name: {{ include "lise-serverless.fullname" . }}
  labels:
{{ include "lise-serverless.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "lise-serverless.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
