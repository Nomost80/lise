apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lise-serverless.fullname" . }}-test-connection"
  labels:
{{ include "lise-serverless.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lise-serverless.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
